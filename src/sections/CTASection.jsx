export default function CTASection() {
  return (
    <section className="sec sec-red sec-cta">
      <h2 className="cta-h">Ready to <em>rewrite</em><br/>the inside of your car?</h2>
      <p className="cta-p">Limited builds per month. Reserve a consultation slot before the diary closes.</p>
      <div className="cta-btns">
        <button className="cta-btn cta-btn-dark">Book your build</button>
        <button className="cta-btn cta-btn-ghost">View the portfolio</button>
      </div>
    </section>
  )
}
